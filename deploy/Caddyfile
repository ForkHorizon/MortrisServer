# Local dev only. Caddy issues an internal certificate so the smoke test
# exercises the same HTTPS shape as production without a public DNS name.
{
	admin off
}

https://localhost:8443 {
	tls internal
	request_body {
		max_size 256KB
	}
	reverse_proxy app:8080
}
